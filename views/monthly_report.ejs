<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Monthly Attendance Report</title>

    <% if (!isPDF) { %>
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
    <% } %>

    <style>
        body {
            <% if (isPDF) { %>
                background-color: #ffffff;
                color: #000000;
            <% } else { %>
                background-color: #13203c;
                color: #ffffff;
            <% } %>
            font-family: Arial, sans-serif;
        }

        table {
            width: 100%;
            border-collapse: collapse;
        }

        table thead {
            background-color: <% if (isPDF) { %>#e9ecef<% } else { %>#2479f0<% } %>;
            color: <% if (isPDF) { %>#000<% } else { %>#fff<% } %>;
        }

        th, td {
            padding: 8px;
            border: 1px solid #ddd;
            font-size: 13px;
            text-align: center;
        }

        table tbody td {
            <% if (!isPDF) { %>
                background-color: #2e518a;
            <% } %>
        }

        .footer {
            margin-top: 40px;
            text-align: center;
            font-size: 12px;
            opacity: 0.7;
        }
    </style>
</head>

<body class="p-4">
<div class="container-fluid">

    <h3>📅 Monthly Attendance Report</h3>
    <h5><%= monthName %></h5>

    <% if (!isPDF) { %>
        <div class="mb-3">
            <a href="/admin" class="btn btn-light">← Back</a>
        </div>
    <% } %>

    <div class="table-responsive">
        <table>
            <thead>
                <tr>
                    <th>Employee ID</th>
                    <th>Name</th>
                    <th>Date</th>
                    <th>Punch In</th>
                    <th>Punch Out</th>
                    <th>In Location</th>
                    <th>Out Location</th>
                    <th>Absent</th>
                    <th>Reason</th>
                </tr>
            </thead>
            <tbody>
                <% records.forEach(r => { %>
                    <tr>
                        <td><%= r.emp_id %></td>
                        <td><%= r.name %></td>
                        <td><%= r.date %></td>
                        <td><%= r.time_in %></td>
                        <td><%= r.time_out %></td>
                        <td><%= r.location_in %></td>
                        <td><%= r.location_out %></td>
                        <td><%= r.absent %></td>
                        <td><%= r.reason %></td>
                    </tr>
                <% }) %>
            </tbody>
        </table>
    </div>

    <div class="footer">
        Generated on <%= new Date().toLocaleString() %> |
        Smart Attendance System
    </div>

</div>
</body>
</html>