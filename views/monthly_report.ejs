<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Monthly Attendance Report</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
    <style>
        body {
            background-color: #13203c;
            color: #fff;
        }

        .table thead {
            background-color: #2479f0;
            color: #fff;
        }

        .table tbody td {
            background-color: #2e518a;
        }
    </style>
</head>

<body class="p-4">
    <div class="container-fluid">
        <h3 class="mb-4">📅 Monthly Attendance Report — <%= month %>
        </h3>
        <a href="/admin" class="btn btn-light mb-3">← Back to Dashboard</a>

        <div class="table-responsive">
            <table class="table table-bordered table-striped table-hover">
                <thead>
                    <tr>
                        <th>Employee ID</th>
                        <th>Name</th>
                        <th>Date</th>
                        <th>Punch In</th>
                        <th>Punch Out</th>
                        <th>In Location</th>
                        <th>Out Location</th>
                        <th>Absent</th>
                        <th>Reason</th>
                    </tr>
                </thead>
                <tbody>
                    <% records.forEach(r=> { %>
                        <tr>
                            <td>
                                <%= r.emp_id %>
                            </td>
                            <td>
                                <%= r.name %>
                            </td>
                            <td>
                                <%= r.date %>
                            </td>
                            <td>
                                <%= r.time_in %>
                            </td>
                            <td>
                                <%= r.time_out %>
                            </td>
                            <td>
                                <%= r.location_in %>
                            </td>
                            <td>
                                <%= r.location_out %>
                            </td>
                            <td>
                                <%= r.absent %>
                            </td>
                            <td>
                                <%= r.reason %>
                            </td>
                        </tr>
                        <% }); %>
                </tbody>
            </table>
        </div>
    </div>
</body>

</html>